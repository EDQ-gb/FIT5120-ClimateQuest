{
  "name": "fit5120-climatequest",
  "private": true,
  "description": "Root scripts; app lives in frontend/ and server/",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "dev:local": "concurrently -k -n server,vite -c blue,green \"npm run dev:server\" \"npm run dev\"",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:server": "npm run dev --prefix server",
    "build": "npm run build --prefix frontend",
    "install:all": "npm install --prefix frontend && npm install --prefix server && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
